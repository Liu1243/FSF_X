# 第3章 方法设计

## 3.1 问题定义与总体框架

面向超远距离稀疏感知场景，本文研究多模态输入条件下的全稀疏三维目标检测问题。设输入为同步采集的多视角图像与激光雷达点云，分别记为
[
\mathcal{I}={I_1,I_2,\dots,I_M}, \quad \mathcal{P}={p_i}*{i=1}^{N_p},
]
其中，(M) 表示相机数量，(N_p) 表示点云点数。本文目标是在保持计算复杂度可控的前提下，输出场景中目标的三维边界框、类别标签及其置信度，即
[
\mathcal{Y}={(b_k,c_k,s_k)}*{k=1}^{N_o},
]
其中，(b_k) 表示第 (k) 个目标的三维框参数，(c_k) 为类别标签，(s_k) 为预测置信度，(N_o) 为目标数量。

与传统稠密 BEV 检测方法不同，本文采用全稀疏实例表示作为统一中间表征，仅保留场景中具有潜在目标意义的稀疏 token 或实例区域参与后续建模。尽管该策略能够显著降低无效区域带来的计算开销，但也引入了新的挑战：其一，稀疏实例在三维空间中呈离散无序分布，难以直接适配状态空间模型的序列建模机制；其二，多模态实例之间存在明显的几何失配、置信度差异和局部退化现象，简单融合容易导致噪声传播；其三，在超远距离条件下，目标尺寸小、点云稀疏、遮挡严重，传统局部聚合难以有效建模长距离依赖关系。

针对上述问题，本文提出一种**基于空间序列化、前景增强与可靠性门控的层次化 H-Mamba 特征交互框架**。整体流程包括六个阶段：
（1）输入与稀疏预处理；
（2）前景增强与动态 token 筛选；
（3）稀疏实例空间序列化；
（4）层次化双向 Mamba 特征交互；
（5）可靠性建模与跨模态融合；
（6）稀疏检测头输出。

其中，前景增强模块用于在序列化之前抑制大规模背景噪声；空间序列化模块用于将离散三维实例映射为具备局部邻域保持性的一维序列；层次化 H-Mamba 模块负责完成模态内建模、跨模态对齐与全局上下文回流；可靠性门控模块根据点云密度、深度波动、遮挡程度和距离分布自适应调节融合强度，从而提高复杂工况下的稳定性。整体框架实现了从“稀疏实例构建—有效 token 选择—结构化序列建模—可信跨模态融合—检测输出”的完整方法闭环。

---

## 3.2 稀疏输入表征与统一实例构建

为了为后续跨模态特征交互提供统一输入，本文首先分别从图像模态和 LiDAR 模态中提取稀疏特征表示，并将其转换为统一的稀疏实例集合。

对于图像分支，输入多视角图像首先经二维视觉骨干网络提取多尺度语义特征，随后通过视角变换或几何投影操作，将图像中的语义响应映射至三维空间候选区域中。该过程保留了图像模态在纹理、边缘、类别语义和轮廓信息方面的优势。对于 LiDAR 分支，输入点云首先经过体素化与稀疏卷积主干网络编码，构建带有显式空间几何位置的三维稀疏表示，从而保留深度结构、局部几何形状与空间分布信息。

设图像分支输出的稀疏实例集合为
[
\mathcal{X}^{cam}={x_i^{cam}}*{i=1}^{N_c},
]
LiDAR 分支输出的稀疏实例集合为
[
\mathcal{X}^{lid}={x_j^{lid}}*{j=1}^{N_l},
]
其中 (x_i^{cam}) 和 (x_j^{lid}) 均包含特征向量、三维坐标、尺度信息及候选置信度等属性，(N_c) 与 (N_l) 分别表示两种模态下的稀疏实例数量。为便于后续统一建模，本文进一步将两路实例映射到共享坐标系下，并以“实例 token”的形式组织为后续模块的基础输入。

该阶段的核心作用在于：一方面，建立统一的稀疏实例表达格式，使不同模态能够在相同结构下参与序列建模；另一方面，最大程度保留图像语义与 LiDAR 几何之间的互补性，为后续实例筛选和交互融合奠定基础。

---

## 3.3 前景增强与动态 Token 筛选

在自动驾驶场景中，场景中真正属于目标的区域通常远少于背景区域，尤其在超远距离感知条件下，大量 token 对检测无贡献甚至会引入噪声。如果直接将全部稀疏 token 输入后续序列模型，不仅会增加计算负担，还可能削弱有效目标的表达能力。因此，本文在空间序列化之前引入前景增强与动态 token 筛选模块，用于对高价值实例进行优先保留。

具体地，对于每个候选稀疏实例 (x)，本文设计一个前景目标性评分函数
[
o(x)=f_{obj}(x),
]
其中 (f_{obj}(\cdot)) 表示由轻量前景评分网络实现的 objectness 估计函数，其输入可由几何特征、语义特征和上下文响应共同构成。评分值 (o(x)\in[0,1]) 表示该 token 属于前景目标的概率。训练过程中，前景评分分支可采用 focal loss 或其变体作为监督，以提高在前景稀疏、类别不均衡条件下的判别能力。

在获得前景评分后，本文基于排序与裁剪机制保留前景相关性更强的 token。设所有候选实例构成集合 (\mathcal{X})，则筛选后的实例集合定义为
[
\mathcal{X}_f=\text{TopK}(\mathcal{X}, o, K),
]
其中 (\text{TopK}) 表示按前景评分选取前 (K) 个实例。(K) 可设置为固定比例，也可根据场景复杂度动态调整。通过这一机制，本文在尽量保留有效目标信息的同时，显著压缩背景 token 数量，从而降低后续 H-Mamba 模块的序列长度和噪声干扰。

该模块的意义不仅在于削减计算量，更在于提高后续序列交互的有效性：经过筛选的 token 更集中于潜在目标区域，使得序列模型的表达能力能够优先分配给真正影响检测性能的实例。在方法层面，这一步为“线性复杂度可落地”提供了重要前提。

---

## 3.4 稀疏实例空间序列化

状态空间模型擅长处理结构化的一维序列，但三维场景中的稀疏实例本质上是无序离散分布的。为使稀疏实例适配 H-Mamba 模型，本文设计基于三维空间拓扑保持的稀疏实例空间序列化方法，将原始三维实例集合映射为一维结构化序列，同时尽可能保留局部邻域关系。

### 3.4.1 基于 Hilbert 曲线的三维序列构造

设前景筛选后的稀疏实例集合为 (\mathcal{X}*f={x_i}*{i=1}^{N_f})，每个实例对应三维坐标 ((u_i,v_i,w_i))。本文采用 3D Hilbert 空间填充曲线对实例进行排序，定义序列化映射为
[
\pi: (u_i,v_i,w_i)\rightarrow t_i,
]
其中 (t_i) 表示实例在一维序列中的排序位置。经排序后得到的结构化序列记为
[
\mathcal{S}={x_{\pi^{-1}(1)},x_{\pi^{-1}(2)},\dots,x_{\pi^{-1}(N_f)}}.
]

相比于简单的字典序排序或规则栅格扫描，Hilbert 序列化更能保持空间局部邻接关系，即空间中彼此接近的实例在序列中也更可能相邻。这一性质对于状态空间模型尤为重要，因为其局部状态传递和长程记忆能力在很大程度上依赖序列局部结构是否与真实空间拓扑一致。

### 3.4.2 Z 轴多角度旋转补偿

单一 Hilbert 曲线的扫描方式可能对特定空间朝向存在偏置，特别是在道路场景中，目标分布往往呈现明显的方向性。为减弱序列化方向偏差对模型的影响，本文引入基于 z 轴旋转的多角度补偿策略。对于旋转角 (\theta\in{0,\pi/2,\pi,3\pi/2})，对实例坐标进行变换
[
\mathbf{p}_i^{(\theta)}=\mathbf{R}_z(\theta)\mathbf{p}_i,
]
其中 (\mathbf{R}_z(\theta)) 表示绕 z 轴的旋转矩阵。随后分别对不同旋转后的坐标执行 Hilbert 序列化，并在训练阶段随机采样或在推理阶段进行稳健聚合，从而增强序列建模对场景方向变化的适应能力。

### 3.4.3 区域到全局滑窗序列组织

在超长距离场景中，即便经过前景筛选，稀疏实例序列长度仍可能较长。为兼顾局部交互效率与全局建模能力，本文进一步引入从区域到全局的滑窗式层次化组织策略。具体而言，首先在局部窗口内对实例进行子序列建模，再通过窗口间信息传递逐步扩展到更大范围，从而实现局部到全局的层次化序列表示。该策略既有助于保留局部空间细节，也降低了一次性建模超长序列带来的状态传播不稳定问题。

综上，空间序列化模块不仅解决了三维无序实例与序列模型之间的结构不匹配问题，还为后续层次化 H-Mamba 建模提供了具备空间拓扑保持特性的输入基础。

---

## 3.5 层次化双向 H-Mamba 特征交互模块

在完成稀疏实例空间序列化后，本文构建层次化双向 H-Mamba 特征交互模块，用于实现模态内长程依赖建模、跨模态精确对齐及语义辅助上下文融合。该模块由三个递进阶段组成：状态空间融合（State Spatial Fusion, SSF）、多模态 token 对齐（Multi-Modal Token Alignment, MTA）和语义辅助融合（Semantic-Assisted Fusion, SAF）。

### 3.5.1 状态空间融合阶段

序列化后的稀疏实例虽然具备了一维结构，但纯序列建模可能忽略三维局部空间结构。为同时利用状态空间模型的长程建模能力和稀疏卷积的空间结构表达优势，本文首先设计状态空间融合阶段。

设输入序列为
[
\mathcal{S}={s_1,s_2,\dots,s_T}, \quad s_t\in\mathbb{R}^d,
]
其中 (T) 为序列长度，(d) 为特征维度。本文首先使用双向 Mamba 模块对序列进行状态更新：
[
\overrightarrow{h_t}=\text{Mamba}*f(s_t,\overrightarrow{h*{t-1}}),\qquad
\overleftarrow{h_t}=\text{Mamba}*b(s_t,\overleftarrow{h*{t+1}}),
]
并将正反向状态进行融合得到序列隐状态
[
h_t=[\overrightarrow{h_t};\overleftarrow{h_t}],
]
其中 ([\cdot;\cdot]) 表示特征拼接。

随后，本文将隐状态重新散射回稀疏三维张量空间，执行按维度分解的稀疏卷积操作，以恢复局部几何邻接关系并增强空间感受野，最后再将其展平回一维序列。这一“序列—空间—序列”的双向映射机制使模型既能利用状态空间模型建模长程依赖，又能保留三维局部几何结构，从而得到几何增强的序列状态表示。

### 3.5.2 多模态 Token 对齐阶段

经过状态空间融合后，图像与 LiDAR token 分别获得了更充分的模态内表达，但二者之间仍可能存在几何位置偏差与量化误差。为此，本文设计多模态 token 对齐阶段，对不同模态实例进行显式位置修正和对应关系建模。

设图像 token 坐标为 (\mathbf{q}*i^{cam})，LiDAR token 坐标为 (\mathbf{q}*j^{lid})，本文通过轻量对齐网络预测两模态之间的偏移量：
[
\Delta \mathbf{q}=f*{align}(h_i^{cam},h_j^{lid}),
]
其中 (f*{align}(\cdot)) 可由 MLP 或门控映射实现。随后利用该偏移量对 token 坐标进行更新：
[
\hat{\mathbf{q}}_i^{cam}=\mathbf{q}_i^{cam}+\Delta \mathbf{q},\qquad
\hat{\mathbf{q}}_j^{lid}=\mathbf{q}_j^{lid}-\Delta \mathbf{q}.
]
通过这种对称修正方式，模型能够减小两种模态在空间对齐上的误差，提高后续交互的有效性。

为控制训练显存，跨模态对应关系的搜索采用按块最近邻策略。具体而言，图像 token 与 LiDAR token 的距离代价和语义相似度代价只用于确定离散 partner index，因此该路由过程不参与梯度反传；实现时对 query token 分块计算最近邻，避免一次性构造完整的 (N_{cam}\times N_{lid}) 代价矩阵。确定 partner 后，仍通过对齐 MLP 对 token 特征进行可导的偏移更新，从而在保持对齐学习能力的同时降低显存峰值。

该阶段的关键作用在于：将跨模态融合由“粗粒度关联”提升为“实例级显式对齐”，从而避免将几何不一致的 token 强行融合作为同一目标处理，提升拥挤场景和远距离目标下的匹配准确性。

### 3.5.3 语义辅助融合阶段

仅有几何对齐并不足以保证有效融合，空间上接近的两个实例未必属于同一语义目标。为此，本文进一步引入语义辅助融合阶段，在对齐后的 token 上显式注入语义先验，增强真正相关实例之间的交互强度。

具体地，本文首先基于对齐后的 token 预测语义类别分布：
[
\mathbf{p}*i=\text{Softmax}(f*{sem}(h_i)),
]
其中 (f_{sem}(\cdot)) 表示语义预测分支。随后，利用语义分布对 token 序列进行语义引导重排序，使类别相关性更高的实例在局部上下文中获得更强的聚合机会。之后，通过一维卷积或扩展感受野卷积进一步提取语义辅助上下文表示，最后再映射回原始序列顺序，得到语义增强的融合特征。

该阶段的引入使得跨模态特征交互不再仅依赖几何邻接关系，而是能够同时考虑语义相似性和上下文一致性，从而提高在边界模糊、目标密集或局部遮挡场景下的表达稳定性。

---

## 3.6 可靠性建模与不确定性感知跨模态融合

多模态融合的有效性依赖于不同模态特征的可信程度。然而在真实自动驾驶场景中，不同模态常因距离、遮挡、稀疏采样和成像条件变化而呈现显著的质量差异。若不加区分地进行统一融合，低质量模态可能向高质量模态传播错误信息，进而损害检测精度。基于此，本文在层次化 H-Mamba 模块之后设计可靠性建模与不确定性感知融合模块。

### 3.6.1 可靠性描述子构建

本文从几何质量、成像稳定性与场景条件三个层面构建可靠性描述子。对于每个实例 token，定义其可靠性特征为
[
r_i=[\rho_i,\tau_i,o_i,d_i],
]
其中，(\rho_i) 表示 LiDAR 局部点密度，(\tau_i) 表示图像深度估计或视锥深度方差，(o_i) 表示遮挡程度，(d_i) 表示目标距离。该描述子用于刻画不同模态在当前空间位置上的观测质量和不确定性来源。

### 3.6.2 空间可靠性门控

基于可靠性描述子，本文设计空间位置相关的门控函数：
[
\gamma_i=\sigma(f_{gate}(r_i)),
]
其中 (f_{gate}(\cdot)) 为轻量门控网络，(\sigma(\cdot)) 为 Sigmoid 函数，(\gamma_i\in(0,1)) 表示实例在融合时对某一模态的信任程度。利用该门控系数，对两模态特征进行自适应加权融合：
[
z_i=\gamma_i \cdot h_i^{cam} + (1-\gamma_i)\cdot h_i^{lid}.
]
当图像模态观测更可靠时，(\gamma_i) 更大；当 LiDAR 几何更完整时，(\gamma_i) 更小。该机制能够在局部空间中动态平衡两种模态的贡献，避免统一融合策略带来的信息偏置。

### 3.6.3 不确定性感知逆方差融合

为进一步增强融合的理论合理性，本文额外引入方差预测分支，对各模态的不确定性进行显式估计。设图像模态和 LiDAR 模态对应的预测方差分别为 (\sigma_{cam,i}^2) 与 (\sigma_{lid,i}^2)，则基于逆方差原则的融合可表示为
[
z_i=\frac{\sigma_{lid,i}^{-2} h_i^{cam}+\sigma_{cam,i}^{-2} h_i^{lid}}
{\sigma_{cam,i}^{-2}+\sigma_{lid,i}^{-2}}.
]
该形式体现了“低方差高可信，高方差低可信”的基本思想，使融合权重具有明确的统计意义。实际实现中，逆方差融合可与门控机制联合使用，从而同时兼顾结构性先验与数据驱动不确定性估计。

### 3.6.4 时序一致性补偿

考虑到自动驾驶系统中常见的异步采样、运动目标漂移和局部瞬时噪声问题，本文进一步引入时序 Mamba 模块对相邻帧特征进行轻量级时序建模。通过在时间维度上传播隐状态，模型能够利用历史观测对当前帧不稳定区域进行补偿，从而提高动态目标和远距离目标的时间一致性。该设计使得本文方法不仅适用于单帧静态融合，也能够在连续感知场景中保持更稳定的检测表现。

---

## 3.7 稀疏检测头与训练目标

在获得经空间序列化、层次化 H-Mamba 交互及可靠性融合后的稀疏实例表示后，本文采用轻量级稀疏检测头完成最终预测。设融合后的实例特征集合为
[
\mathcal{Z}={z_i}_{i=1}^{N_f},
]
检测头分别输出类别概率、三维框参数回归量以及目标置信度，即
[
\hat{c}*i=f*{cls}(z_i),\qquad
\hat{b}*i=f*{reg}(z_i),\qquad
\hat{s}*i=f*{conf}(z_i).
]

整体训练目标由分类损失、边界框回归损失、前景评分损失和对齐/可靠性相关辅助损失组成：
[
\mathcal{L}=\lambda_1\mathcal{L}*{cls}+\lambda_2\mathcal{L}*{reg}
+\lambda_3\mathcal{L}*{obj}+\lambda_4\mathcal{L}*{align}
+\lambda_5\mathcal{L}*{rel},
]
其中，(\mathcal{L}*{cls}) 为分类损失，(\mathcal{L}*{reg}) 为三维框回归损失，(\mathcal{L}*{obj}) 为前景评分监督，(\mathcal{L}*{align}) 用于约束多模态 token 对齐质量，(\mathcal{L}*{rel}) 用于稳定可靠性估计与不确定性建模过程，(\lambda_1\sim\lambda_5) 为各项损失的平衡系数。

这一联合优化方式使各子模块不再是彼此孤立的附加组件，而是在统一检测目标驱动下协同训练，从而形成从特征构建到目标预测的端到端优化框架。

---

## 3.8 方法特点与复杂度分析

与传统基于全局注意力的跨模态交互方法相比，本文方法的主要特点体现在以下三个方面。

首先，在输入层面，本文采用全稀疏实例表示并通过前景增强进行有效 token 筛选，显著减少了参与全局建模的无效背景区域。其次，在交互层面，本文通过空间序列化将三维无序实例映射为拓扑保持的一维序列，使状态空间模型能够以近线性复杂度进行长程依赖建模。再次，在融合层面，本文通过可靠性门控与不确定性感知机制解决了多模态观测质量不一致的问题，使融合过程更符合真实感知条件下的可信性需求。

从复杂度角度看，若参与交互的前景 token 数量为 (N_f)，则传统全局自注意力的复杂度通常为 (O(N_f^2))，而本文基于 H-Mamba 的主干序列建模复杂度近似为 (O(N_f))。在经过前景裁剪后，(N_f) 本身已被有效压缩，因此整体方法在大范围场景中更适合部署于算力受限的平台。需要说明的是，本文方法中的局部稀疏卷积、对齐网络和可靠性门控虽然引入了额外开销，但其总体复杂度远低于全局注意力项，不改变主干线性复杂度优势。

---

## 3.9 本章小结

本章围绕超远距离全稀疏多模态三维目标检测任务，提出了一种基于空间序列化、前景增强与可靠性门控的层次化 H-Mamba 特征交互方法。该方法首先通过图像与 LiDAR 双分支构建统一稀疏实例表示，并利用前景评分与动态裁剪抑制背景噪声；随后通过 3D Hilbert 序列化、旋转补偿和区域到全局滑窗策略，将离散三维实例组织为具有空间拓扑保持性的一维序列；在此基础上，构建由状态空间融合、多模态 token 对齐和语义辅助融合组成的层次化双向 H-Mamba 交互模块，实现从模态内建模到跨模态语义增强的递进式特征交互；最后，结合可靠性描述子、门控融合、不确定性估计和时序一致性补偿，提升复杂场景下的融合稳定性，并通过稀疏检测头输出最终三维检测结果。整体上，本文方法形成了面向真实自动驾驶场景的高效、稳定、鲁棒的稀疏跨模态检测技术路线。

---

## 3.10 nuScenes mini 实验结果分析与后续优化

### 3.10.1 实验结果解读

在 `FSF_HierMamba_nuScenes_mini_config.py` 的 6 epoch mini 实验中，模型取得 `mAP=0.5112`、`NDS=0.5424`。从整体指标看，模型已经能够在主要动态目标上形成有效检测能力：`car`、`truck`、`bus` 和 `pedestrian` 的 AP 分别达到 0.867、0.800、0.981 和 0.906，且这些类别的中心定位误差整体较低，说明 H-Mamba 交互并未破坏 FSF 原有的几何定位能力，在车辆和行人等高频类别上表现稳定。

但该结果不能直接按完整 nuScenes 验证集的方式解读。当前 mini 验证集只有 81 帧，本地 `nuscenes_infos_val.pkl` 中可被当前评估类表识别的 GT 主要集中在 `car`、`pedestrian`、`motorcycle`、`truck`、`bicycle`、`bus` 和 `traffic_cone`，没有有效的 `trailer`、`construction_vehicle` 与 `barrier` GT。因此这三类的 AP 为 0 首先是 mini split 类别缺失导致的评估偏差，而不是 HierMamba 主体结构已经失效的直接证据。若只统计有有效 AP 的 7 个类别，平均 AP 约为 0.7304，能够更真实地反映当前 mini 实验中可验证类别的检测能力。

同时，结果中仍然暴露出两个真实短板。第一，虽然 `trailer`、`construction_vehicle` 和 `barrier` 在 mini val 中缺少有效 GT，但预测文件中仍存在这些类别的大量输出，说明模型的长尾类别置信度校准不足，容易在缺少对应目标的场景中产生误报。第二，`bicycle` 的 AP 只有 0.217，且 `bicycle`、`motorcycle` 的朝向误差分别达到 0.791 和 0.620，`traffic_cone` 的尺度误差达到 0.397，说明小目标、细长目标和弱几何目标仍是当前方法的主要瓶颈。`bus` 的速度误差为 0.398，也提示动态属性分支在 mini 训练轮数较少时仍不稳定。

因此，本次实验的结论应概括为：H-Mamba 对高频车辆和行人类别有效，当前 mAP 被 mini 验证集类别缺失显著压低；真正需要优先优化的是长尾类别误报校准、小目标姿态/尺度回归以及类别感知的 token 保留策略。

### 3.10.2 优化方向与优先级

后续优化不宜直接推翻 H-Mamba 主体，而应按“评估修正—训练校准—轻量模型增强”的顺序推进。

第一，先修正评估协议。除标准 mAP/NDS 外，应额外报告 present-class mAP，即只在当前 split 中存在有效 GT 的类别上计算平均 AP；同时统计每类 GT 数、预测数、匹配 TP 数和 FP 数。对于 mini 实验，应明确标注 `trailer`、`construction_vehicle`、`barrier` 这类缺失类别，避免将 split 偏差误判为模型退化。正式结论仍需在完整 nuScenes val 或类别均衡的 mini 子集上验证。

第二，进行训练与后处理校准。当前配置中 mini 实验只训练 6 epoch，适合作为连通性验证，但不足以稳定长尾类别和动态属性。后续应对比 6 epoch、12 epoch 与完整 20 epoch schedule；同时对不同类别组设置独立的 score threshold、NMS 阈值和输出数量上限，尤其抑制在无 GT 场景中的 `trailer`、`construction_vehicle` 与 `barrier` 误报。对于 `barrier` 这类细长静态目标，还需要检查匹配半径、聚类距离和分配代价是否过严，避免训练阶段无法形成稳定正样本。

第三，在模型层面引入类别感知的 H-Mamba token 选择与小目标几何增强。当前前景筛选更接近通用 objectness top-k，容易优先保留高置信度大目标，而丢失低置信度的两轮车、交通锥、施工车辆等困难目标。应将筛选策略从全局 top-k 改为类别组配额与可靠性共同驱动的选择机制，并在 H-Mamba 交互后增加轻量级小目标姿态/尺度修正分支。

### 3.10.3 建议写入模型的改进设计

为解决上述问题，本文方法可在不改变主干框架的前提下补充三个轻量改动。

（1）类别感知的前景 token 筛选。将原有前景分数扩展为
[
s_i=\alpha o_i+\beta \max_c p_i^c+\eta q_i+\mu r_i,
]
其中 (o_i) 为 objectness，(p_i^c) 为类别置信度，(q_i) 为跨模态投影一致性或 2D 检测置信度，(r_i) 为可靠性描述子。随后不再只执行全局 TopK，而是按类别组设置最小保留配额：
[
K_g=\max(K_g^{min}, \lceil \rho_g N_g\rceil),
]
其中 (g) 表示类别组，(K_g^{min}) 用于保证长尾类和小目标类在序列化前不会被完全裁剪。这样可以降低 `bicycle`、`motorcycle`、`traffic_cone`、`construction_vehicle` 等类别在早期 token 选择阶段被高频车辆类压制的风险。

在实际实现中，类别组配额采用两阶段 soft quota fallback。首先统计由模态最低保留策略已经选中的目标组 token，若已满足 (K_g^{min})，则不再额外占用名额；若仍存在缺口，则优先从预测类别满足 (\arg\max_c p_i^c\in g) 的候选中按组内最大置信度
[
\max_{c\in g}p_i^c
]
补齐。若训练早期分类分布不稳定，导致满足硬类别条件的候选不足，则进一步从剩余 token 中按同一组内最大置信度回退选择。该 soft fallback 能保留对类别明确候选的优先级，同时避免真实小目标因 argmax 类别暂时错误而在序列化前被裁剪。

（2）面向小目标与细长目标的几何可靠性增强。在原有可靠性描述子中补充局部点数、2D 框长宽比、投影覆盖率、类别熵和实例尺度先验，将可靠性特征扩展为
[
r_i=[\rho_i,\tau_i,o_i,d_i,a_i,e_i,\kappa_i],
]
其中 (a_i) 表示尺寸或长宽比先验，(e_i) 表示类别分布熵，(\kappa_i) 表示 2D-3D 投影一致性。该描述子既参与融合门控，也参与类别置信度校准，使模型在交通锥、两轮车和 barrier 这类弱几何目标上减少尺度漂移和类别误报。

（3）小目标姿态与尺度残差修正分支。在 H-Mamba 输出特征后，为 `bicycle`、`motorcycle`、`traffic_cone` 和 `barrier` 等困难类别增加共享的轻量残差头，专门回归朝向残差、尺度残差和置信度温度系数。朝向可采用 `sin/cos` 形式约束周期性，尺度残差引入类别均值先验，置信度温度系数用于抑制无对应 GT 场景下的长尾误报。该分支只作用于小目标和细长目标，不增加高频大目标的额外负担。

### 3.10.4 后续消融实验设计

为了验证优化是否真实有效，后续实验应至少包含以下消融：

- `FSF baseline`、`FSF_HMamba`、`FSF_HMamba + 类别感知 token 筛选`、`FSF_HMamba + 几何可靠性增强`、`FSF_HMamba + 小目标残差修正` 的逐步对比。
- `keep_ratio` 取 0.5、0.75、1.0 的对比，用于判断当前 token 裁剪是否伤害长尾目标。
- `num_rotations` 取 1、2、4 以及 `window_size` 取 16、32、64 的对比，用于验证多方向 Hilbert 序列化和区域到全局窗口是否真正带来收益。
- mini split 上同时报告标准 mAP、present-class mAP、每类 GT 数、每类预测数和每类 FP 数；完整 val 上重点观察 `trailer`、`construction_vehicle`、`barrier` 是否从 0 AP 恢复，并观察 `bicycle`、`motorcycle` 的 AOE 以及 `traffic_cone` 的 ASE 是否下降。

预期上，若类别感知 token 筛选有效，`bicycle`、`motorcycle` 和 `traffic_cone` 的 AP 应优先提升；若几何可靠性增强和残差修正有效，`bicycle/motorcycle` 的朝向误差与 `traffic_cone` 的尺度误差应明显下降；若置信度校准有效，mini val 中缺失类别的预测数和 FP 数应减少，而 present-class mAP 不应下降。
